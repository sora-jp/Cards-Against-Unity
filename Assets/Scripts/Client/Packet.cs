// If this code works, it was written by Sora
// Otherwise, I don't know who wrote it

internal struct Packet
{
    public object messageID;
    public IMessageData messageData;
    public bool logPacket;
}